{
  "_comment": "SWCanvas Node text-test fixture spec. scripts/build-smoke-fixture.js reads this and regenerates the sibling QOI atlases + bundle subsets in this directory from the downloaded WebP font-assets release (npm run text:build-smoke-fixture). Edit this file to change the fixture's fonts/densities, then regenerate and commit. Three fonts at densities 1 and 2, size 16: enough to exercise the full drawTextFromAtlas path including the font-invariant character branch, at both atlas densities.",
  "fontSets": [
    {
      "name": "Smoke set — Arial 16 (normal + bold)",
      "density": [1, 2],
      "families": ["Arial"],
      "styles": ["normal"],
      "weights": ["normal", "bold"],
      "sizes": [16]
    },
    {
      "name": "Smoke set — BitmapTextInvariant 16",
      "density": [1, 2],
      "families": ["BitmapTextInvariant"],
      "styles": ["normal"],
      "weights": ["normal"],
      "sizes": [16]
    }
  ]
}
